<div class="min-h-screen bg-slate-50"><div class="max-w-7xl mx-auto p-6"><div class="mb-6"><h1 class="text-2xl font-bold">Users</h1><p class="text-slate-500">Manage account access and role assignments.</p></div>@if(session('message'))<div class="mb-4 p-3 rounded-lg bg-emerald-50 text-emerald-700">{{session('message')}}</div>@endif
<div class="flex flex-wrap gap-3 mb-4"><input wire:model.live.debounce.300ms="search" placeholder="Search name or email" class="rounded-lg border-slate-300 flex-1 min-w-64"><select wire:model.live="status" class="rounded-lg border-slate-300"><option value="all">All</option><option value="active">Active</option><option value="inactive">Inactive</option></select></div>
<div class="bg-white rounded-2xl border overflow-x-auto"><table class="min-w-full divide-y"><thead class="bg-slate-50"><tr>@foreach(['User','Organisation','Roles','Subscription','Status','Actions'] as $h)<th class="px-4 py-3 text-left text-xs uppercase text-slate-500">{{$h}}</th>@endforeach</tr></thead><tbody class="divide-y">@foreach($users as $user)<tr><td class="px-4 py-3"><div class="font-medium">{{$user->name}}</div><div class="text-xs text-slate-500">{{$user->email}}</div></td><td class="px-4 py-3 text-sm">{{$user->organisation?->name ?? '—'}}</td><td class="px-4 py-3"><div class="flex flex-wrap gap-1">@foreach($user->roles as $role)<button wire:click="removeRole({{$user->id}},{{$role->id}})" class="text-xs px-2 py-1 rounded-full bg-indigo-50 text-indigo-700" title="Click to remove">{{$role->name}} ×</button>@endforeach</div><select wire:change="assignRole({{$user->id}}, $event.target.value)" class="mt-2 text-xs rounded border-slate-300"><option value="">Add role...</option>@foreach($roles as $role)<option value="{{$role->id}}">{{$role->name}}</option>@endforeach</select></td><td class="px-4 py-3 text-sm">@php($sub=$user->subscriptions->sortByDesc('created_at')->first()){{$sub?->plan?->name ?? 'None'}} @if($sub)<span class="text-xs text-slate-500">({{$sub->status}})</span>@endif</td><td class="px-4 py-3"><span class="text-xs px-2 py-1 rounded-full {{$user->is_active?'bg-emerald-100 text-emerald-700':'bg-red-100 text-red-700'}}">{{$user->is_active?'Active':'Inactive'}}</span></td><td class="px-4 py-3"><button wire:click="toggleActive({{$user->id}})" wire:confirm="Change this user's access status?" class="text-sm {{$user->is_active?'text-red-600':'text-emerald-600'}}">{{$user->is_active?'Disable':'Enable'}}</button></td></tr>@endforeach</tbody></table><div class="p-4">{{$users->links()}}</div></div></div></div>
